{
  "name": "portfolio-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "predeploy": "cd portfolio && npm install && npm run build",
    "deploy": "gh-pages -d portfolio/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
